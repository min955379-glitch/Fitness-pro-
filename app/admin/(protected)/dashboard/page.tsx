import Link from 'next/link'
import {ArrowRight,CalendarCheck,Clock3,CreditCard,MessageSquare,UsersRound} from 'lucide-react'
import {AdminPageHeading} from '@/components/admin-ui'
import {EmptyState,StatusPill} from '@/components/ui'
import {requireAdmin} from '@/lib/site'
export const metadata={title:'Dashboard | Fitness Pro Admin'}
export default async function Dashboard(){const {supabase,user}=await requireAdmin();const [{count:members},{count:pending},{count:messages},{count:today},{count:payments},{data:registrations}]=await Promise.all([supabase.from('members').select('*',{count:'exact',head:true}).neq('membership_status','removed'),supabase.from('members').select('*',{count:'exact',head:true}).eq('approval_status','pending'),supabase.from('contact_messages').select('*',{count:'exact',head:true}).eq('status','new'),supabase.from('attendance').select('*',{count:'exact',head:true}).eq('attendance_date',new Date().toISOString().slice(0,10)).eq('status','present'),supabase.from('payment_requests').select('*',{count:'exact',head:true}).eq('status','pending'),supabase.from('members').select('id,full_name,email,approval_status,created_at,membership_plans(name)').order('created_at',{ascending:false}).limit(6)]);const firstName=user.user_metadata?.full_name||user.email?.split('@')[0]||'Admin';return <><AdminPageHeading eyebrow="Control center" title={`Welcome, ${firstName}`} copy="A live overview of members, gym operations and website enquiries."/><div className="adminMetrics"><Link href="/admin/members"><span><UsersRound/>Members</span><strong>{members??0}</strong><small>Active directory <ArrowRight/></small></Link><Link href="/admin/registrations"><span><Clock3/>Pending approval</span><strong>{pending??0}</strong><small>Review registrations <ArrowRight/></small></Link><Link href="/admin/messages"><span><MessageSquare/>New messages</span><strong>{messages??0}</strong><small>Open inbox <ArrowRight/></small></Link><Link href="/admin/attendance"><span><CalendarCheck/>Present today</span><strong>{today??0}</strong><small>Mark attendance <ArrowRight/></small></Link><Link href="/admin/payments"><span><CreditCard/>Payment requests</span><strong>{payments??0}</strong><small>Verify payments <ArrowRight/></small></Link></div><div className="adminDashboardGrid"><section className="adminCard"><div className="adminCardHeader"><div><span className="eyebrow">Latest</span><h2>Recent registrations</h2></div><Link className="textLink" href="/admin/registrations">View all <ArrowRight/></Link></div>{registrations?.length?<div className="adminList">{registrations.map(r=><Link href={`/admin/members/${r.id}`} key={r.id}><span className="avatar">{r.full_name.slice(0,1).toUpperCase()}</span><span><strong>{r.full_name}</strong><small>{r.email||'No email'} • {new Date(r.created_at).toLocaleDateString('en-PK')}</small></span><StatusPill value={r.approval_status}/></Link>)}</div>:<EmptyState title="No registrations yet" copy="New online registrations will appear here."/>}</section><section className="adminCard quickAdmin"><div className="adminCardHeader"><div><span className="eyebrow">Shortcuts</span><h2>Common tasks</h2></div></div><Link href="/admin/members/new"><UsersRound/><span><strong>Add a member</strong><small>Create a walk-in member record.</small></span><ArrowRight/></Link><Link href="/admin/attendance"><CalendarCheck/><span><strong>Mark attendance</strong><small>Record today&apos;s member visits.</small></span><ArrowRight/></Link><Link href="/admin/gallery"><MessageSquare/><span><strong>Update website</strong><small>Add a new gallery image.</small></span><ArrowRight/></Link><Link href="/admin/settings"><CreditCard/><span><strong>Business settings</strong><small>Contact, hours and website copy.</small></span><ArrowRight/></Link></section></div></>}
