'use client'
import {useState} from 'react';import {createClient} from '@/lib/supabase-browser';import {useRouter} from 'next/navigation'
export default function Login(){const [email,setEmail]=useState('min955378@gmail.com'),[password,setPassword]=useState(''),[error,setError]=useState('');const router=useRouter();async function go(e:React.FormEvent){e.preventDefault();const {error}=await createClient().auth.signInWithPassword({email,password});if(error)setError(error.message);else router.push('/admin')}return <main className="admin" style={{display:'grid',placeItems:'center',padding:20}}><div className="card pad" style={{width:'min(430px,100%)'}}><div className="brand"><span className="brandmark">FP</span><span>FITNESS <b style={{color:'var(--yellow)'}}>PRO</b></span></div><h1>Admin Login</h1><p className="small">Private management portal.</p><form className="formgrid" onSubmit={go}><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Admin email"/><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" required/><button className="btn">Sign In</button>{error&&<div style={{color:'#ff7676'}}>{error}</div>}</form></div></main>}
