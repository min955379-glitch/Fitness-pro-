import {Clock3,Mail,MapPin,MessageCircle,Phone} from 'lucide-react'
import {ContactForm} from '@/components/contact-form'
import {SiteShell} from '@/components/site-shell'
import {PageHero,SectionHeading} from '@/components/ui'
import {getPublicData} from '@/lib/site'
export const metadata={title:'Contact | Fitness Pro Abbottabad',description:'Contact Fitness Pro in Nawan Shehr, Abbottabad. Ask about memberships, coaching and gym visits.'}
const faqs=[['Can I visit before joining?','Yes. Contact the team to plan a quick facility tour and discuss the best training time.'],['Do I need gym experience?','Not at all. New members can ask for guidance on equipment, basic technique and a sensible starting routine.'],['How does online registration work?','Create an account and choose an optional plan. The team reviews your registration, then approves membership access.'],['Can I pay at the gym?','Yes. Online payment requests include a pay-at-the-gym option. The team confirms each payment manually.']]
export default async function Contact(){const {settings}=await getPublicData();return <SiteShell settings={settings}><main><PageHero eyebrow="Contact Fitness Pro" title="Let's talk about your next step." copy="Ask about a plan, arrange a visit or tell us what support you need."/><section className="section"><div className="container contactLayout"><div><SectionHeading eyebrow="Send a message" title="How can we help?" copy="Messages are reviewed by the Fitness Pro team during opening hours."/><ContactForm/></div><aside className="contactAside"><div><MessageCircle/><h2>Visit or get in touch</h2><p>We are happy to answer questions before you commit.</p></div><a href={`tel:${settings.phone.replace(/\s/g,'')}`}><Phone/><span><small>Phone / WhatsApp</small>{settings.phone}</span></a><a href={`mailto:${settings.email}`}><Mail/><span><small>Email</small>{settings.email}</span></a><div><MapPin/><span><small>Location</small>{settings.address}</span></div><div><Clock3/><span><small>Monday – Friday</small>{settings.weekday_hours}<small>Saturday – Sunday</small>{settings.weekend_hours}</span></div><a className="button" href={`https://wa.me/${settings.whatsapp}`} target="_blank" rel="noreferrer">Open WhatsApp</a></aside></div></section><section className="section sectionTint"><div className="container"><SectionHeading eyebrow="Common questions" title="Good to know"/><div className="faqGrid">{faqs.map(([q,a])=><details key={q}><summary>{q}</summary><p>{a}</p></details>)}</div></div></section><section className="mapPanel"><div className="container"><MapPin/><div><span className="eyebrow">Fitness Pro • Abbottabad</span><h2>{settings.address}</h2><p>Open Monday to Friday for morning and evening training sessions.</p></div><a className="button buttonGhost" target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(settings.address)}`}>Get directions</a></div></section></main></SiteShell>}
