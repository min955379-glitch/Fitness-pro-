import {createClient} from '@/lib/supabase-server';import Link from 'next/link';import {redirect} from 'next/navigation'
export default async function Requests(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/admin/login');const {data}=await supabase.from('membership_requests').select('*').order('created_at',{ascending:false});return <main className="admin"><aside className="sidebar"><div className="brand"><span className="brandmark">FP</span><span>FITNESS PRO</span></div><div className="sideLinks"><Link href="/admin">Dashboard</Link><Link className="active" href="/admin/requests">Membership Requests</Link><Link href="/">View Website</Link></div></aside><section className="adminMain"><div className="eyebrow">Leads</div><h1 className="title" style={{fontSize:52}}>Membership Requests</h1><div className="card pad"><table className="table"><thead><tr><th>Name</th><th>Phone</th><th>Email</th><th>Plan</th><th>Status</th><th>Created</th></tr></thead><tbody>{(data??[]).map((r:any)=><tr key={r.id}><td>{r.name}</td><td><a href={'https://wa.me/92'+r.phone.replace(/^0/,'')} target="_blank">{r.phone}</a></td><td>{r.email||'-'}</td><td>{r.plan||'-'}</td><td>{r.status}</td><td>{new Date(r.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></section></main>}
